# MatchMaker — 组织者快速上手指南

**Languages**: [English](ORGANIZER_GUIDE.md) · 简体中文

🔗 **App 地址**：**<https://CaiZhou2.github.io/MatchMaker/>**

你正在组织一个每周的小型比赛，有人把 MatchMaker 分享给你了。这份指南能让你在 10 分钟内从零开始办完第一场比赛 —— 不需要写代码，不需要注册账号。

> **一句话说明**：用手机浏览器打开上面那个 URL → 添加到主屏幕 → 开始第一场比赛。**每场结束后一定要导出备份。** 就这么简单。

---

## 这是个什么东西？

MatchMaker 是一个跑在手机浏览器里的小工具（不需要应用商店、不需要登录、比赛过程中不需要联网）。它做四件事：

1. **管理选手名单**，跨周累积积分和胜率
2. **自动平衡组队**，按每个人的胜率抽队长 + 随机分配队友
3. **自动或手动选赛制**，可以让 App 自动选，也可以从小组赛+淘汰赛 / 纯循环赛 / 纯单败淘汰 / 纯友谊赛 4 种里手动指定
4. **比赛中录入结果**，结束时一键写入所有人的积分和消费
5. **仅记录模式**（可选）—— 跳过组队，手动逐场记录比赛，每场独立控制是否计入积分
6. **比赛预测** —— 选两支队伍的选手，查看胜/平/负概率预测（基于对位数据和综合胜率）

App 支持**多项目管理** —— 比如"周二羽毛球"和"周末篮球"可以各建一个项目，选手和数据互不干扰。所有数据**只存在你这台手机上**，不会上传到任何服务器，其他组织者看不到你的数据。

> 🤝 **友谊第一。** MatchMaker 的所有组队和配对算法都为一个
> 目标服务：让每场比赛**势均力敌**，而不是评出"谁最强"。组队
> 的时候会让两支队伍的总体实力尽量接近（用累计胜率作为简单
> 的 Elo 代理），纯友谊赛模式还会显式循环三种模板 —— 强强
> 对决、弱弱拉锯、强弱混合 —— 让一场赛事下来每个人都和很多
> 不同的对手玩到接近水平的比赛，而不是被卡在同一拨人里反复
> 打或者打一边倒的碾压局。

---

## 第 1 步：装到手机上（一次性，约 2 分钟）

这是一个 PWA（渐进式 Web 应用），可以从一个 URL 像普通 App 一样装到主屏幕 —— 不需要 App Store / 应用市场。

### iOS（iPhone / iPad）

1. **必须用 Safari** 打开 MatchMaker 的 URL（不能用 Chrome —— iOS 只允许 Safari 装 PWA）。
2. 点底部的**分享**按钮（带向上箭头的方框图标）。
3. 在分享菜单里向下滚，点**添加到主屏幕**。
4. 右上角点**添加**。
5. 主屏幕出现一个 🏆 图标。点开它，App 全屏打开，没有浏览器外壳。

### Android

1. 用 Chrome（或 Edge / 三星浏览器 / 任何现代浏览器）打开 MatchMaker 的 URL。
2. 点右上角 **⋮** 菜单。
3. 点**安装应用**（或**添加到主屏幕**）。
4. 确认安装对话框。
5. 应用抽屉和主屏幕都会出现 🏆 图标。

### 验证装好了

- 完全关闭 Safari / Chrome
- 点主屏幕的图标
- 应用应该全屏打开（看不到 URL 栏）
- 试试**打开飞行模式**再打开应用 —— 应该还能用。能用就说明装好了，**完全离线可用**。

---

## 第 2 步：创建项目（一次性）

第一次打开 App 会看到**项目列表**页面。一个项目就是一个独立的比赛环境 —— 有自己的选手、历史和排行榜。

1. 点 **新建项目**，输入名称（比如"周六羽毛球"）。
2. 进入项目主页，之后所有操作都在这个项目内。

> 💡 如果你有不同的活动（比如周二羽毛球、周末篮球），给每个活动建一个项目就行。主页左上角的 **← 返回** 按钮可以回到项目列表。

> 💡 老用户升级后，原有数据会自动迁移到一个叫"我的比赛"的默认项目中，不会丢失。

---

## 第 3 步：建立选手名单（一次性）

这一步只需要做一次。以后新加入的人随时往里加就行。

1. 打开 App → 点 **📇 选手数据库**
2. 输入姓名，点**添加**。把你这一组的所有人都加一遍。
3. 列表会显示每个人的积分、胜率、参赛周数和累计消费。第一次都是零。

> 💡 不一定要一次性把所有人都加进去。在比赛设置页面有个**快速添加**输入框，可以在新人第一次出现的时候直接添加。

---

## 第 4 步：办第一场比赛（设置 5 分钟，然后开打）

1. 主页 → **开始本周比赛**
2. **勾选今天到场的人**，顶部会显示已选人数
3. 设置参数：
   - **每队人数** —— 双打 2，单打 1
   - **场地数** —— 你今天有几块场地
   - **每场时长（分钟）** —— 羽毛球一般 15 分钟
   - **总时间（分钟）** —— 场馆订到几点
   - **比赛模式** —— 5 选 1：
     - **自动 (推荐)** —— 让 App 帮你挑。它会依次尝试小组赛+淘汰、循环赛、纯友谊赛。**永远能给出可行的方案。**
     - **小组赛 + 淘汰赛** —— 显式指定先打小组赛再打淘汰。**至少需要 6 支队伍**才能分 2 个组。
     - **纯小组赛 (循环赛)** —— 每队和其他所有队各打一场。
     - **纯淘汰赛 (单败)** —— 标准单败淘汰表，输一场就出局。**要求队伍数为 2 的幂次**（2/4/8/16/32），凑不齐 App 会提示。
     - **纯友谊赛 (不计积分)** —— 没有固定的周队伍。每场比赛会按胜率重新组队保证公平。**不发放比赛积分**，所以积分榜不动；但**胜 / 平 / 负仍然会记录**，所以胜率榜和对位胜率仍会更新 —— 友谊赛仍然影响水平评估，只是不影响排名分数。适合休闲场、混合水平热身、"今天就是来玩玩"的场子。
4. **本周消费** —— 场馆费用（人民币或别的，App 不在乎单位）。完成比赛时会自动均摊到每个参赛者。
5. 点**生成队伍**

> ⚠️ 4 种显式模式（除"自动"以外）**不会偷偷回退**。如果你选的模式装不下当前的人数 / 时间 / 场地，App 会弹窗告诉你具体原因并停在设置页 —— 你需要手动改模式或者改参数。**只有"自动"模式能保证一定有方案。**

### 你会看到什么

- **队伍** —— 自动生成的阵容。👑 标记是队长（每队胜率最高的那个）。每队 1 个队长 + 随机分配的队友，所以队伍之间实力大致平衡。*（纯友谊赛不一样 —— 看下面的友谊赛说明。）*
- **友谊赛说明** —— 如果你选了纯友谊赛（或者自动模式回退到了它），顶部会出现说明横幅，告诉你每场比赛的队伍都会重新组合，所有结果都不计积分。下面显示的是按时段排列的比赛预览，而不是固定的队伍卡片。
- **赛制预览** —— 在当前设置下，App 会用什么赛制运行（X 场比赛 · Y 个时段 · 约 Z 分钟）。
- **🔄 重新随机** —— 重新抽一次（友谊赛模式下也可以用，重新生成随机配对）
- **🔀 手动调整** —— 点两个选手互换位置（仅固定队伍模式可用）
- **📋 复制比赛安排** —— 把赛程复制成纯文本，粘贴到群里（微信群、QQ 群、Telegram 都行），所有人都能看

确认阵容满意后，点**开始比赛**。

---

## 替代方案：仅记录模式（不自动组队）

不需要平衡组队和杯赛流程？也许你们就是自由打球，只想记录谁和谁打了什么比赛、保持数据跟踪。**仅记录模式**跳过组队和赛制环节，让你手动逐场记录。

在比赛设置页面（第 3 步），"生成队伍"按钮下方有一个独立的 **📝 仅记录模式** 按钮。点它就行。

**操作方式：**

1. 进入后是一个空白页面 + 一个"**➕ 添加一场比赛**"按钮。
2. 点一下，出现一张空白的比赛卡片。每张卡片有：
   - **选手名字条** —— 点名字依次循环：未选 → A队（蓝色）→ B队（绿色）→ 未选。同一个人可以在不同比赛里分属不同队。
   - **比分输入框** + **A胜 / 平局 / B胜** 按钮 —— 跟正赛一样，填完两个比分会自动判断结果。
   - **"本场计入积分"复选框** —— 默认打勾。取消勾选则本场为友谊赛（胜/平/负仍然记录，但不发放积分）。
   - **删除按钮** (×) —— 删掉这张卡片。
3. 想加多少比赛就加多少。**所有卡片在提交前都可以随时修改** —— 换人、改比分、改结果、改是否计积分，随便改。
4. 打完了，点 **✅ 完成 & 提交所有比赛**。App 会检查每张卡片是否两队都有人、结果已录入。如果有遗漏，会精确提示（"第 3 场尚未填写完整：…"），不会提交。
5. 确认后所有比赛一次性写入数据库 —— 自动备份 + 结果页和正赛完全一致。

> 💡 仅记录模式和杯赛流程共享完全相同的底层提交逻辑。积分、胜率、对位胜率、参与度、消费均摊都完全一致 —— 唯一的区别是比赛怎么来的（手动 vs 自动排期）。

---

## 第 5 步：比赛中录入结果

比赛页面按时间段显示所有比赛。每场比赛：

- **A胜 / 平局 / B胜** 三个按钮，一点就记录
- 也可以在两个数字框里直接输入比分（比如 `21` 和 `15`），结果会**自动**根据比分判断（高的赢，相等是平局）
- 再点同一个结果按钮可以清除（误点了用）

顶部进度条显示 `已完成 / 总场次`。所有比赛都录入后，**完成比赛 & 更新积分** 按钮才会出现。

> 💡 录入比分是可选的。如果今天打得快，只用三个按钮就够了。比分的好处是：小组赛积分相同时用净胜分作为决胜规则，历史详情里也好看一些。

---

## 第 6 步：完成比赛（最重要的一步）

点**完成比赛 & 更新积分** → 确认。

App 会自动做三件事：

1. **把所有人的新积分 / 胜负 / 消费写入数据库**。提交之后这一步在 App 里就撤销不了了。
2. **自动下载一份备份文件**到你手机的下载文件夹，文件名是 `matchmaker-backup-YYYY-MM-DD.json`。成功页面顶部会出现绿色的"💾 备份已自动下载"提示。
3. **显示一个 📤 分享备份 按钮**（如果你的手机支持的话）。点一下就能用微信 / 邮件 / AirDrop 把备份文件发出去。

### ⚠️ 这一段一定要看 —— 决定了你以后会不会哭

**每场比赛后都把自动下载的备份发到一个安全的地方。** 最简单的流程：

- 点 **📤 分享备份** → 选 **微信** → 选 **文件传输助手**
- 完事。备份现在在云端了，任何设备都能拿到，免费，永久。
- 或者：发到自己邮箱、AirDrop 到笔记本、保存到 iCloud Drive、夸克网盘、百度网盘 …… 都行。

**为什么这件事这么重要：**

- iOS Safari 有一个叫 ITP 的"智能"隐私功能，**7 天没打开 App 就可能清掉所有数据**。如果你跨过一周（节假日、出差），可能下次打开发现一切归零。
- 换手机 / 清理浏览器 / 浏览器抽风都可能让数据全没。
- App 内部有保护层（数据会镜像备份到一个更耐用的存储里），但**只有你聊天记录里的那个文件是 100% 可靠的备份**。

**最坏情况发生时**（打开 App 发现选手清零了）：看下面的"故障排查"。

---

## 第 7 步：日常查看数据

主页有三个排行榜 Tab：

- **积分榜** —— 累计积分（胜 = 3 / 平 = 1 / 负 = 0）
- **胜率榜** —— 胜场 ÷ 总场次
- **参与榜** —— 参与周数最多（适合评"年度勤勤恳恳奖"之类的）

在**📇 选手数据库**里点任意选手，或者用**🔍 选手查询**搜，可以看到选手详情页：完整的统计数据 + 对战每个对手的"对位胜率"表格。

主页的消费统计卡片显示累计总消费。**复制消费信息**会把每个人的消费明细复制成文本，发到群里（"这是大家本月的费用"）。**清零消费**重置所有人的累计消费（比如月初清零）—— 在下次比赛提交前可以一键恢复。

### 比赛预测

消费统计下方是**比赛结果预测**工具。点击选手名字分配到 A 队（蓝色）或 B 队（绿色）—— 再点一次循环 `A → B → 未选`。两队都有人后，会显示一个概率条，展示预计的胜/平/负概率。

预测优先使用**对位数据**（所选选手之间过去实际的对战记录）。如果对位数据不足，会回退到**比较综合胜率**。如果有选手比赛场次不足 3 场，会提示"数据不足"。

> 💡 这是一个辅助参考工具，不是绝对准确的预测。可以用来在组队前大致看看阵容是否平衡。

---

## 软件更新

开发者推送新版本时，App 顶部会弹出 **"✨ 有新版本可用 / 立即更新"** 横幅。点**立即更新**，App 就会重新加载到新版本。**你的数据在更新中不会丢**。

如果你觉得应该有新版本但没看到横幅：把 App 完全关闭（从最近任务里左滑或上滑划掉），再重新打开，横幅就会出现。

---

## 换手机 / 数据丢了之后导入

1. 把备份的 JSON 文件传到新手机（比如从微信文件传输助手下载）
2. 在新手机上打开 MatchMaker
3. 点 **📇 选手数据库** → 滚到**数据导入 / 导出** → 点 **📥 导入 JSON**
4. 选择那个备份文件。App 会显示 "X 个选手, Y 条历史 — 确认？" → 确认
5. 完成。所有数据回来了。

---

## 故障排查

**App 显示零选手，但我昨天还有数据。**
- iOS Safari 可能因为 ITP 7 天清理策略清掉了 localStorage
- App 的 IndexedDB 影子备份**应该**会在下次打开时自动恢复数据。如果是的话，你只会看到选手们悄悄出现
- 如果没自动恢复，导入你最新的 JSON 备份（看上面"换手机"那一节）

**iOS 上没法添加到主屏幕。**
- 必须用 Safari，不能用 Chrome 或别的浏览器。iOS 只允许 Safari 装 PWA。

**完成比赛页面没有 📤 分享备份 按钮。**
- 你的浏览器不支持 Web Share API 文件分享。但 App 应该已经把备份自动下载到下载文件夹了 —— 从那里手动分享就行。

**录入比分后没有自动选结果。**
- 两个比分都要填。两个数字都填了之后，结果会自动判断（高的赢，相等是平局）。

**点错了结果按钮。**
- 点正确的那个，新值会覆盖旧值。（再点同一个按钮会**清除**结果 —— 也可以用这个方法撤销重输。）

**我要改选手姓名 / 合并两个选手。**
- 目前只能用：导出 → 手工编辑 JSON 文件 → 导入。App 还没有内建的改名和合并功能。

**比赛装不下我的时间预算。**
- 如果在**自动**模式下，App 会自动回退（小组赛+淘汰 → 循环赛 → 纯友谊赛），有横幅说明。
- 如果你**显式**选了某种模式（小组赛+淘汰 / 循环赛 / 纯淘汰 / 纯友谊），App 会弹窗告诉你具体不行的原因并阻止下一步。可选项：换模式（最简单是换成自动）、加时间、加场地、改人数。

**纯淘汰赛说我的队伍数不是 2 的幂次。**
- 单败淘汰只接受 2/4/8/16/32 支队伍以保证淘汰表的整洁。要么换成"小组赛+淘汰赛"（任意队伍数都行），要么加/减一个人凑到 2 的幂次。

---

## 反馈

如果有 bug 或者哪里不清楚，去 [GitHub 仓库](../../)开 issue。Pull request 也欢迎 —— 看主 [README](../README.zh-CN.md) 的贡献说明。

如果 MatchMaker 对你的小组有帮助，**给项目点个 star ⭐**，能帮到其他正在找类似工具的组织者。
